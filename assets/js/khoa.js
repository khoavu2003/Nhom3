document.addEventListener("DOMContentLoaded", function () {
    const profileCard = document.querySelector(".fade-in");

    setTimeout(() => {
        profileCard.classList.add("visible");
    }, 200);
});
